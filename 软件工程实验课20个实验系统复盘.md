# 软件工程实验课 20 个实验系统复盘

> 适用对象：已经完成实验、但希望重新建立“会分析、会设计、会实现、会验证”的软件工程能力的学习者。  
> 复盘原则：不只记住代码，而是能够从需求出发，选择数据结构和算法，说明复杂度，处理异常，并用测试证明程序可靠。

## 0. 如何使用本复习文档

每个实验按四个问题复盘：

1. **实验目标**：题目最终要解决什么问题。
2. **核心知识点**：它对应哪些可迁移的软件工程/计算机科学知识。
3. **工程实操要点**：真正写程序时应如何拆分、实现、验证。
4. **常见踩坑**：哪些错误会导致结果错、崩溃、泄漏或难以维护。

建议每完成一个章节，都做三件事：

- 不看源码，画出数据流和函数调用图；
- 写出状态、边界条件和时间/空间复杂度；
- 为正常、边界、非法输入各设计至少一个测试用例。

---

# 第一部分：20 个实验逐个复盘

## 实验 1：基础算法、函数与大数表示

### 实验目标

`class_1` 包含三个递进练习：用迭代计算兔子繁殖数列；用数组模拟乘法输出 `1!` 到 `40!`；用函数和二维数组输出杨辉三角。

### 软件工程核心知识点

- **需求建模**：兔子问题先确定递推关系 `F(n)=F(n-1)+F(n-2)`，再确定初始条件。
- **抽象与封装**：杨辉三角由 `print_pascals_triangle` 独立负责，主函数只处理输入和调用。
- **表示与实现分离**：阶乘超出整数范围后，用“低位在前”的十进制数组表示大整数。
- **循环不变量**：每次乘法结束时，数组低 `result_len` 位保存当前阶乘，`carry` 是尚未写回的进位。

### 工程实操要点

- 先写公式、状态含义和边界，再写循环；不要直接凭感觉修改变量。
- 大数乘法逐位执行 `temp = digit * multiplier + carry`，保存 `temp % 10`，向后传 `temp / 10`，最后处理剩余进位。
- 对杨辉三角，明确 `triangle[i][0]` 和 `triangle[i][i]` 恒为 1，其余位置来自上一行。
- 测试：兔子至少验证第 1、2、12 月；阶乘验证 `0!`、`1!`、`10!`、`40!`；三角形验证 0/1/多行。

### 常见踩坑与易错点

- 把“第 12 个月的数量”和“12 个月内累计数量”混为一谈。
- 忘记初始化局部数组；未初始化的 `result` 会产生未定义结果。
- 大数数组容量写死却没有检查上限；扩展到更大阶乘时可能越界。
- 变长数组 `int triangle[rows][rows]` 依赖 C99，且 `rows <= 0` 时行为不符合预期。
- 递推变量更新顺序错误会丢失上一状态；应先计算，再依次滑动。

## 实验 2：结构体建模与随机发牌

### 实验目标

把一副 54 张扑克牌建模为 `Card` 数组，初始化、洗牌、向 3 名玩家发牌并保留 3 张底牌，重复 5 轮输出结果。

### 核心知识点

- **结构体建模**：把花色和牌面组合成一个领域对象。
- **模块化**：初始化、洗牌、发牌、展示分别由函数完成。
- **随机算法**：使用 Fisher-Yates 思想从后向前交换，避免简单随机交换造成分布偏差。
- **伪随机种子**：`srand` 只初始化一次，随后调用 `rand`。

### 工程实操要点

- 先建立不变量：牌数 `4*13+2=54`，每张牌只出现一次。
- 发牌下标要明确：玩家 `i` 的第 `j` 张牌对应 `deck[i*17+j]`。
- 用宏集中表达数量，减少魔法数字，便于改变玩家数和牌数。
- 验证：统计每轮牌面总数、检查重复牌、检查两轮是否因种子位置不同而变化。

### 常见踩坑与易错点

- 在每轮洗牌中都 `srand(time(NULL))`，同一秒内会得到相同种子，导致多轮结果相同。
- `strcpy` 目标数组长度估计不足；应使用足够容量或安全复制。
- 只验证“看起来随机”，却没有验证 54 张牌是否完整、是否重复。
- 将牌的“逻辑编号”和数组下标混淆，造成少牌或越界。

## 实验 3：IPv4、IPv6、MAC 地址校验

### 实验目标

编写字符串校验函数，区分并验证 IPv4、IPv6、MAC 地址，并报告错误段位置。

### 核心知识点

- **词法与语法校验**：字符集、段数、段长度、数值范围、前导零都是不同规则。
- **字符串分词**：使用分隔符扫描输入，理解 `strtok` 的破坏性和状态。
- **输出参数**：通过 `errSeg` 向调用者返回错误位置。
- **输入防御**：外部字符串必须受到长度、字符集和终止符约束。

### 工程实操要点

- IPv4：恰好 4 段、只含数字、单段 `0` 合法但不能有前导零、范围 0~255。
- IPv6：恰好 8 段、每段 1~4 个十六进制字符；本实验题设明确禁止 `::` 压缩。
- MAC：恰好 6 段，每段 2 个十六进制字符，分隔符应统一或明确允许混用。
- 更稳健的做法是写统一的“扫描器”，显式识别空段、连续分隔符、末尾分隔符，而不是完全依赖 `strtok`。

### 常见踩坑与易错点

- `strcpy(ipCopy, ip)` 没有检查输入长度，长输入可造成栈缓冲区溢出。
- `strtok` 会跳过连续分隔符，可能把空段误判为不存在；因此无法可靠检测 `1..2.3`。
- `isdigit`/`isxdigit`/`tolower` 应传入 `unsigned char` 转换后的值，避免负 `char` 导致未定义行为。
- `scanf("%s")` 没有宽度限制；应使用 `scanf("%99s", input)` 或 `fgets`。
- 主函数中三个校验器复用同一个 `errSeg`，失败时应保证错误位置语义清楚。

## 实验 4：两个有序数组的归并

### 实验目标

将两个已升序排列的数组合并为一个升序数组，允许重复值。

### 核心知识点

- **双指针/归并思想**：每次取两个当前元素的较小者。
- **循环不变量**：`result[0..k)` 始终是已处理元素中的有序前缀。
- **边界处理**：主循环结束后，两个数组至多有一个仍有剩余。
- **复杂度意识**：时间 `O(n+m)`，额外空间 `O(n+m)`。

### 工程实操要点

- 使用 `i、j、k` 分别表示两个输入数组和输出数组位置。
- 比较相等时固定选择一侧，保证实现稳定且不会漏元素。
- 校验 `0 <= n,m < 100`，输出数组容量至少为 200。
- 测试空数组、一个数组为空、全部相同、负数和交错元素。

### 常见踩坑与易错点

- 合并时只写主循环，忘记复制剩余尾部。
- 使用 `<=` 却错误地增加两个指针，导致重复元素丢失。
- 把“合并”误写成去重；题目要求保留重复值。
- 没有验证输入数组本身有序，导致算法前提被破坏。

## 实验 5：单向链表、双向链表、单向循环链表

### 实验目标

建立三种链表，支持升序插入、按值删除、查询、遍历、菜单操作和退出时释放内存。

### 核心知识点

- **动态内存与所有权**：节点由 `malloc` 创建，由链表删除或销毁函数 `free`。
- **指针不变量**：单链表维护 `next`；双链表同时维护 `next/prior`；循环链表尾节点回到头节点。
- **头指针修改**：插入/删除头结点时必须通过二级指针修改调用者的头指针。
- **接口设计**：把创建、插入、删除、查询、显示、销毁分离，菜单只负责调度。

### 工程实操要点

- 插入前先确定三类位置：空表、头部、中间/尾部。
- 双链表插入或删除时，同时检查前驱和后继，维护双向一致性。
- 循环链表遍历使用 `do...while`，终止条件是“再次回到 head”，不能以 `NULL` 为终点。
- 删除操作保存待释放节点的后继，避免 `free` 后继续解引用。
- 测试空表、单节点、头尾节点、重复值、删除全部重复值、连续插入删除。

### 常见踩坑与易错点

- 删除循环链表头节点后忘记更新尾节点的 `next`。
- 双链表只更新 `next`，忘记同步 `prior`，导致反向遍历崩溃。
- `display` 从空循环链表开始寻找尾节点，空指针解引用。
- 删除后继续使用已释放指针，形成 use-after-free。
- 只在菜单退出时释放，异常返回路径或新增函数中可能产生泄漏；应提供统一 `destroy`。

## 实验 6：二叉搜索树及多种遍历

### 实验目标

建立二叉搜索树，支持插入、删除、中序/前序/后序/层序/显式栈深度优先遍历，并释放整棵树。

### 核心知识点

- **递归定义**：树的操作天然适合递归，空树是递归基。
- **BST 不变量**：左子树值小于根，右子树值大于根；本实现拒绝重复值。
- **删除三种情况**：叶子、只有一个孩子、左右孩子都存在。
- **遍历策略**：递归 DFS、队列 BFS、显式栈 DFS 的结构差异。

### 工程实操要点

- 删除双子节点时，用右子树最小节点（中序后继）替换，再递归删除后继。
- 遍历前先明确访问顺序；BST 中序遍历应输出有序序列，可作为正确性断言。
- 释放树使用后序遍历，先释放子树再释放父节点。
- 理论复杂度平均为 `O(log n)` 查找/插入/删除，最坏退化为 `O(n)`；构建平衡树能改善初始形态。

### 常见踩坑与易错点

- 删除根节点后忘记写回 `root = deleteBST(root, val)`。
- 层序队列容量固定为 100，超过容量会越界；工程实现应动态扩容或按节点数分配。
- 递归树过深可能栈溢出，极端有序输入尤其明显。
- 删除不存在节点却无提示，界面反馈与真实状态不一致。

## 实验 7：中缀表达式转后缀并求值

### 实验目标

使用运算符栈把中缀表达式转换为后缀表达式，再使用数字栈求值，支持多位整数、括号和四则运算。

### 核心知识点

- **栈的典型应用**：运算符优先级、括号匹配、后缀求值。
- **词法分析**：区分连续数字组成的操作数、空白和运算符。
- **错误处理**：操作数不足、除零、括号不匹配、非法字符和表达式剩余元素。
- **结合性**：相同优先级下，左结合运算符应弹出栈顶。

### 工程实操要点

- 转换规则：数字直接输出；运算符弹出优先级不低于当前运算符的栈顶；右括号弹到左括号。
- 求值时先弹 `b` 再弹 `a`，计算 `a op b`，减法和除法不能反过来。
- 明确本实验只支持非负整数；若要支持一元负号，需扩展词法和语法。
- 设计测试：`8+(1+2)*4`、多位数、嵌套括号、除零、缺少操作数、非法字符。

### 常见踩坑与易错点

- 栈空时直接 `pop`；必须先检查 `top`。
- 输入数组长度固定 100，却没有检查转换结果是否超长。
- 只跳过非法字符并继续计算，会把错误输入伪装成正确结果；工程上应显式报错。
- 左括号未匹配时仍调用 `charPop`，会产生错误访问。

## 实验 8：舞伴循环配对

### 实验目标

用两个循环队列表示男女队伍，每轮从队头配对，配对者回到队尾，并报告人数较多队伍中下一位未配对者。

### 核心知识点

- **队列 FIFO**：先进先出，配对完成后重新入队实现循环。
- **环形队列**：通过取模让固定数组空间循环复用。
- **状态不变量**：`front` 指向队头，`rear` 指向下一空位；队列长度为 `(rear-front+MAX)%MAX`。
- **业务规则转算法**：每轮配对数是两队当前人数的最小值。

### 工程实操要点

- 明确环形队列“牺牲一个空槽”时的满队判断。
- 所有字符串复制都必须受 `NAMELEN` 限制，并验证人数不超过容量。
- 每轮配对后，两人重新入队，因此队列人数不减少。
- 测试男多、女多、人数相等、人数为 0、连续多轮和队列接近容量。

### 常见踩坑与易错点

- 把循环队列误当成普通队列，`rear++` 不取模会越界。
- 忽略 `enQueue` 失败返回值，满队时会丢人名。
- 用 `strcpy` 写入固定长度姓名数组导致溢出。
- “未配对者”应来自较长队列；人数相等时要定义输出规则，不能依赖未初始化内容。

## 实验 9：图的邻接表、逆邻接表、DFS 与 BFS

### 实验目标

用出边链表和入边链表表示有向/无向图，打印两种邻接关系，并实现 DFS、BFS 遍历。

### 核心知识点

- **图的表示**：邻接表适合稀疏图，逆邻接表便于查询入边。
- **遍历**：DFS 使用递归栈，BFS 使用队列。
- **访问标记**：避免环导致无限遍历，并处理非连通图。
- **资源管理**：每条动态边都必须有对应的释放路径。

### 工程实操要点

- 有向边 `i -> j` 需要同时加入 `i` 的出链表和 `j` 的入链表。
- 无向图需补充反向出边和入边；边数语义应在文档中说明。
- DFS/BFS 外层遍历所有顶点，才能覆盖非连通分量。
- 测试空图、自环、平行边、孤立点、环图和有向图。

### 常见踩坑与易错点

- 只在连通图上从 0 号顶点遍历，遗漏其他分量。
- 不初始化 `visited`，导致遍历结果不确定。
- 固定队列 `MAXVEX` 却不检查入队上限。
- 构建过程中输入非法顶点时“跳过”，但仍需保证边计数和错误提示语义一致。

## 实验 10：带权图上的最短电影总花费

### 实验目标

建立 4 城市带权无向图，计算每个城市居民选择任一城市观影时的最小“路费 + 票价”。

### 核心知识点

- **Floyd-Warshall**：通过逐步加入中转点求所有点对最短路径。
- **动态规划思想**：`dist[i][j]` 是状态，转移为经过/不经过 `k` 的最小值。
- **建模**：最短路结果不是最终答案，还要叠加目标城市票价。
- **不可达处理**：`INF` 必须与加法运算安全结合。

### 工程实操要点

- 初始化 `dist[i][i]=0`，无边为 `INF`，无向边对称赋值。
- Floyd 三重循环顺序必须是 `k-i-j`。
- 对每个出发城市 `i`，枚举目标 `j`，取 `dist[i][j] + price[j]` 最小值。
- 若图规模变大，单源查询可考虑 Dijkstra，多源全对查询才适合 Floyd。

### 常见踩坑与易错点

- `INF + INF` 可能整数溢出；更新前应判断可达性。
- 忘记把道路设为双向，导致结果不对称。
- 只求“最短路费”，漏加目标城市电影票价。
- 代码中的城市编号从 1 开始，数组却容易按 0 开始访问，必须统一约定。

## 实验 11：飞机销售顺序与堆/贪心

### 实验目标

前 `m` 天不能销售飞机，之后每天销售一架，在满足“已生产才能销售”的约束下，使总存储成本最小。

### 核心知识点

- **贪心交换思想**：单位滞留成本高的飞机应尽早销售。
- **优先队列/最大堆**：动态维护当前已生产飞机中成本最高者。
- **可行性约束**：销售时刻只能从已生产集合中选择。
- **复杂度**：排序 + 每次堆插入/删除，通常为 `O(n log n)`。

### 工程实操要点

- 每个堆元素必须保存完整对象（至少 `c` 和 `day`），不能只保存成本。
- 选定销售顺序后直接累加 `c * (sell_day - produce_day)`，避免用两个大和相减产生溢出。
- 证明贪心：若较小成本排在较大成本之前，交换二者不会增加成本，因此应优先安排较大成本。
- 通过重复成本、销售日早于后续出厂日、`m=0` 等样例验证调度合法性。

### 常见踩坑与易错点

- 当前 `class_11/calss_11.c` 的堆只存 `c`，再通过“第一个相同 c”寻找出厂日；当成本重复时，调试输出和计算含义可能不对应，工程实现应把 `Plane` 放入堆。
- 文件名代码中打开的是 `input4.txt`，错误提示却写 `input.txt`，会误导排查。
- 没有检查堆为空就取 `heap[0]`。
- `int` 计算 `c * day` 在大数据下可能溢出，应使用 `long long`。

## 实验 12：楼房拆除的最少操作次数

### 实验目标

每次选择连续正高度区间，统一降低一个不超过最小高度的值，求把所有高度变为 0 的最少操作，并输出过程。

### 核心知识点

- **分层/连通块思想**：每个当前正数连续段可一次削到该段最低层。
- **贪心与结构变化**：凹陷会把高层区域拆成多个独立连通块。
- **状态模拟**：`cur[]` 是每次操作后的剩余状态。
- **可解释性**：输出 `(l,r,v)` 使算法过程可审计。

### 工程实操要点

- 扫描所有 `cur[i] > 0` 的连续段，求段内最小值并统一减去。
- 统一处理“遇到 0”和“扫描到数组末尾”两种结束条件。
- 用小规模暴力搜索结果验证贪心最优性；不要只看输出像不像最优。
- 更大规模可研究单调栈/分治，把逐轮模拟进一步优化。

### 常见踩坑与易错点

- 区间端点从 0 下标和题目从 1 下标混用。
- 只处理遇到 0 的情况，遗漏数组末尾的正区间。
- 修改数组后没有重新开始扫描，可能跳过新形成的区间。
- 把“每次只能减 1”误读成“可减任意相同正值”，导致操作数严重偏大。

## 实验 13：最长上升子序列 LIS

### 实验目标

求整数序列的最长严格上升子序列长度，并通过前驱数组恢复一组具体方案。

### 核心知识点

- **动态规划状态设计**：`dp[i]` 表示以 `a[i]` 结尾的最优长度。
- **路径恢复**：`pre[i]` 保存最优状态的前驱，而不仅仅保存答案长度。
- **严格与非严格**：条件 `a[j] < a[i]` 决定是严格上升。
- **复杂度改进**：基础 `O(n²)` DP，进阶是 `O(n log n)` 的 tails/binary search。

### 工程实操要点

- 初始化 `dp[i]=1、pre[i]=-1`。
- 对每个 `i` 遍历所有 `j<i`，若可接且更优则更新。
- 找到最大 `dp` 的位置后沿 `pre` 反向回溯，再逆序输出。
- 测试全降序、全升序、重复值、多个等长 LIS 和 `n=1`。

### 常见踩坑与易错点

- 把子串/连续子数组当成子序列；LIS 不要求连续。
- 使用 `<=` 会把题目要求的严格上升变成非严格上升。
- 只输出长度而没有保存前驱，无法恢复方案。
- `n=0` 时 `pos` 和回溯逻辑需有明确约定。

## 实验 14：0-1 背包与方案回溯

### 实验目标

每件物品最多选择一次，在容量限制内最大化价值，并输出一组最优物品编号。

### 核心知识点

- **状态压缩前的二维 DP**：`dp[i][j]` 表示前 `i` 件、容量 `j` 的最优价值。
- **最优子结构**：第 `i` 件物品只有选/不选两种决策。
- **方案恢复**：比较 `dp[i][j]` 与 `dp[i-1][j]` 判断是否选中。
- **空间优化**：若只求价值可压缩为一维，且容量必须逆序遍历。

### 工程实操要点

- 转移：不选继承；能放下时比较 `dp[i-1][j-w[i]]+v[i]`。
- 回溯从 `(n,W)` 开始，选中后令 `j -= w[i]`。
- 明确“任意一组最优解”允许同价值方案不同。
- 测试容量不足、物品重量相同、价值相同、恰好装满和空选择。

### 常见踩坑与易错点

- 把 0-1 背包写成完全背包，错误地重复使用物品。
- 一维 DP 仍正序遍历容量，导致同一件物品被重复计算。
- 回溯条件使用 `>=`，在相等时可能错误标记物品。
- 固定数组边界与输入约束不一致，导致 `n/W` 越界。

## 实验 15：区间调度

### 实验目标

从若干活动中选择最多个互不重叠活动，允许前一个结束时间等于后一个开始时间，并输出编号方案。

### 核心知识点

- **经典贪心**：按结束时间升序，每次选择当前可行且最早结束的活动。
- **交换论证**：最早结束的可行活动不会比任意最优方案的首个活动更差。
- **排序与原编号保留**：排序不应丢失输入编号。
- **复杂度**：排序 `O(n log n)`，扫描 `O(n)`。

### 工程实操要点

- 选择条件是 `start >= last_end`，等号表示不重叠。
- `last_end` 应初始化为足够小的值，或先选第一个可行活动。
- 使用 `long long` 存储时间，防止时间范围扩大后溢出。
- 测试完全重叠、链式相接、同结束时间、活动按乱序输入。

### 常见踩坑与易错点

- 按开始时间或持续时间排序，通常不能保证最优。
- 输出排序后的数组下标而非原始活动编号。
- 忘记保留 `s_i < e_i` 的输入约束检查。
- `qsort` 比较器直接做大整数减法可能溢出；应显式返回 -1/0/1。

## 实验 16：全排列生成与回溯剪枝

### 实验目标

生成互不相同整数的字典序全排列，若新加入元素与当前末尾元素差的绝对值为 1，则提前剪枝。

### 核心知识点

- **回溯模板**：选择、递归、撤销选择。
- **状态表示**：`path` 表示当前解，`used` 表示元素是否已使用。
- **剪枝**：约束只依赖当前末尾元素，因此可以在递归前判断。
- **字典序**：先排序输入，再按升序尝试候选元素。

### 工程实操要点

- 递归出口是 `pathLen == n`，此时输出合法排列。
- 尝试候选前先检查 `used` 和相邻差约束。
- 回溯必须完整恢复 `pathLen` 和 `used[i]`，否则后续分支污染。
- 统计剪枝前后的节点数，直观看到剪枝收益。

### 常见踩坑与易错点

- 只在完整排列生成后检查，虽然结果可能正确，但没有实现题目要求的提前剪枝。
- 排序后比较器写成 `p1-p2`，大整数输入可能溢出。
- 题目说互不相同；若扩展到重复元素，需要按层去重。
- 调试输出过多会改变可读性和性能，正式版本应与诊断日志分离。

## 实验 17：堆优化 Dijkstra 与路径输出

### 实验目标

在非负权有向图中，从源点求到所有点的最短距离，并输出到指定终点的一条具体路径。

### 核心知识点

- **邻接表**：适合 `m` 很大的稀疏图。
- **松弛操作**：若 `dist[u]+w` 更短，则更新 `dist[v]`。
- **堆优化**：小根堆每次取当前最小距离，复杂度 `O((n+m)log n)`。
- **延迟删除**：堆中可能有同一顶点的旧记录，用 `if (d > dist[u])` 跳过。
- **前驱恢复**：`pre[v]=u`，从终点反向回溯。

### 工程实操要点

- 明确 Dijkstra 前提：边权不能为负；有负边应使用 Bellman-Ford 等算法。
- 对不可达终点输出 `INF`/“不可达”，不要把 `pre` 回溯成虚假路径。
- 堆下标从 1 开始时，父子下标公式必须统一。
- 释放邻接表所有边，且在 `malloc` 失败时及时处理。

### 常见踩坑与易错点

- 把“弹出一次”当作顶点永远确定，忽略旧堆记录。
- 堆容量按 `n` 分配，但每条松弛边都可能入堆，容量应按上界或动态扩容。
- `INF + w` 溢出，松弛前要判断 `dist[u] != INF`。
- 终点不可达时仍无条件输出从 `t` 回溯的路径。

## 实验 18：英文文本统计与性能优化

### 实验目标

统计文本字符数、总单词数、不同单词数和前 10 高频词，并将线性查找、二次排序、单字符 I/O 的版本优化为哈希表、`qsort`、`fread` 版本。

### 核心知识点

- **性能分析**：先定位瓶颈，再优化，不能只凭直觉。
- **哈希表**：字符串哈希映射到桶，链表解决冲突，平均查找接近 `O(1)`。
- **I/O 缓冲**：块读取减少函数调用和系统 I/O 开销。
- **动态内存与资源生命周期**：节点、指针数组都要释放。
- **基准测试**：比较优化前后时间和空间，并说明数据规模。

### 工程实操要点

- 统一大小写，定义“单词”的字符规则，并处理文件末尾残留单词。
- 跨 `fread` 块的单词必须保留在缓冲区，不能按块强行截断。
- 收集哈希节点指针后再排序；哈希表解决查找，不直接提供频率顺序。
- 使用 `-Wall -Wextra`，结合 AddressSanitizer/Valgrind 检查越界和泄漏。

### 常见踩坑与易错点

- 固定 `MAX_WORD_NUM` 或 `WORD_LEN` 却不检查上限。
- `isalpha`、`tolower` 的参数未转换为 `unsigned char`。
- 只优化代码却不做同一输入、同一环境的基准测试。
- 哈希冲突处理不完整导致单词丢失；排序比较器用差值可能溢出。
- “只读取一次”如果没有处理块边界，会把一个单词错误拆成两个词。

## 实验 19：学生信息管理系统

### 实验目标

开发命令行学生信息管理系统：添加、删除、查询、修改成绩、显示、排序、读取/保存二进制数据；使用动态数组管理容量。

### 核心知识点

- **领域建模**：`Student` 与 `StudentList` 分离数据和容器状态。
- **动态数组扩容**：容量不足时按倍数 `realloc`，摊销插入成本较低。
- **持久化**：`fread/fwrite` 读写结构体记录，启动加载、退出保存。
- **模块化和输入校验**：输入、业务操作、文件 I/O、显示各自负责。
- **CRUD 生命周期**：记录从创建、修改、查询到删除的完整流程。

### 工程实操要点

- 学号是唯一标识；成绩限制在 0~100；姓名不能为空。
- `realloc` 先接收临时指针，成功后再覆盖原指针，防止失败时丢失原内存。
- 删除数组元素使用 `memmove` 前移后续记录，并减少 `count`。
- 保存时检查 `fwrite` 数量和 `fclose` 结果；退出前统一 `free_list`。
- 测试首次无数据文件、重复学号、非法成绩、空列表、删除最后一项和重新启动加载。

### 常见踩坑与易错点

- 把文件不存在当成致命错误；首次运行应允许创建空列表。
- 直接把 `realloc` 返回值赋给原指针，失败时造成内存泄漏。
- 二进制结构体持久化存在平台布局/字节序兼容问题；跨平台系统应设计明确文件格式。
- 输入混用 `scanf` 和 `fgets` 时残留换行，导致姓名被读为空。
- 排序后如果业务依赖输入顺序，必须明确排序是否持久化以及学号是否保持稳定。

## 实验 20：命令行 Todo 项目与工程化交付

### 实验目标

开发可执行的命令行任务管理系统，支持 `add/list/done/delete/help`、交互模式、文本文件持久化、环境变量指定数据文件，并配套需求分析、测试报告和 AI 协作记录。

### 核心知识点

- **需求到实现的完整生命周期**：需求分析、模块设计、编码、测试、文档。
- **命令解析**：处理空格、引号、多参数和非法命令。
- **状态管理**：任务有 ID、内容和完成状态，增删改查改变状态。
- **持久化与可恢复性**：程序重启后从文件恢复数据。
- **测试设计**：正常路径、错误 ID、空内容、空列表、数据重启验证。

### 工程实操要点

- 将 `main`、命令分发、任务操作、加载保存、输入清理分层。
- 文本格式 `id|content|done` 简单直观，但内容若允许 `|`，必须转义或改用更可靠格式。
- 添加任务后立即保存，删除后重新整理 ID 的策略要在需求中明确。
- 使用 `gcc -std=c11 -Wall -Wextra` 编译，并用独立数据文件做回归测试。
- 记录 AI 生成/修改了什么、为什么修改、如何人工验证；AI 是辅助，不是验收者。

### 常见踩坑与易错点

- `strdup`、`strtok_r` 在不同编译标准/平台上的可移植性需要确认。
- 任务内容含 `|`、超长内容、引号或换行时，简单分隔格式会解析错误。
- `load_tasks` 读取格式异常时静默跳过，可能造成数据丢失；应记录行号和错误。
- 先修改内存再保存失败时，进程继续运行但用户以为已持久化；应明确提示保存失败。
- 删除后重新编号会破坏外部引用；真实产品更适合使用不可复用的稳定 ID。

---

# 第二部分：打通理论与实操的完整知识体系

## 1. 从需求到程序的统一工作流

1. **澄清需求**：输入、输出、约束、非法情况、是否需要持久化。
2. **建立模型**：选择数组、链表、树、图、栈、队列、哈希表或文件格式。
3. **定义不变量**：例如 BST 的左右序关系、队列的 front/rear 关系、DP 状态含义。
4. **选择算法并证明**：说明状态转移、贪心交换论证或遍历完备性。
5. **拆分模块**：输入层、核心逻辑、输出层、资源释放和错误处理。
6. **实现最小正确版本**：先保证功能和边界，再做性能优化。
7. **验证与回归**：编译警告、单元级函数测试、端到端命令测试、异常测试。
8. **复盘交付**：写 README、复杂度、已知限制、测试报告和改进计划。

## 2. 数据结构选择地图

| 问题特征 | 优先考虑 | 本课程对应实验 |
|---|---|---|
| 固定规模、连续访问 | 数组 | 1、4、13、14 |
| 频繁插入/删除、节点数变化 | 链表/动态数组 | 5、19 |
| 后进先出、嵌套结构 | 栈 | 7、16 的递归栈 |
| 先进先出、按层处理 | 队列/环形队列 | 8、9、6 |
| 有序查找与层次关系 | BST | 6 |
| 连接关系与路径 | 邻接表/堆 | 9、10、17 |
| 高频键查找 | 哈希表 | 18 |
| 跨进程保存 | 明确文件格式 | 19、20 |

## 3. 算法思想地图

- **递推/模拟**：实验 1、2、12；关键是状态和不变量。
- **分治/归并**：实验 4；关键是把已解决的有序部分合并。
- **递归遍历**：实验 5、6、9；关键是基例和恢复调用现场。
- **动态规划**：实验 10、13、14；关键是状态定义、转移、初始化、回溯。
- **贪心**：实验 11、15；关键是局部选择为何不会破坏全局最优。
- **回溯与剪枝**：实验 16；关键是搜索状态、可行性判定、撤销。
- **最短路**：实验 10、17；关键是边权前提、松弛和不可达处理。
- **性能优化**：实验 18；关键是复杂度、数据规模、可测量证据。

## 4. 一套可迁移的质量检查清单

### 正确性

- 是否明确了输入约束、下标约定和空输入行为？
- 是否覆盖空结构、单元素、最大规模、重复值、不可达和非法输入？
- 是否能用不变量解释每个循环/递归为何正确？

### 安全性与健壮性

- 所有数组、字符串、堆和动态内存是否有边界检查？
- `malloc/realloc/fopen` 失败是否显式处理？
- 是否避免 use-after-free、double free、整数溢出和除零？
- 是否对 `ctype.h` 函数正确转换字符类型？

### 可维护性

- 函数是否单一职责，命名是否表达业务含义？
- 魔法数字是否集中为宏/常量？
- 错误信息是否能帮助定位，而不是静默忽略？
- 文档、代码、测试用例是否保持一致？

### 性能

- 是否先判断瓶颈再优化？
- 是否能写出时间/空间复杂度？
- 优化后是否在同一输入和环境下测量？
- 是否以可读性、正确性为前提，而不是盲目追求微优化？

## 5. 课程实验最终应沉淀的能力

完成这 20 个实验后，目标不是背下 20 段 C 代码，而是能够：

1. 将自然语言题目转成状态、约束和接口；
2. 根据访问模式选择合适的数据结构；
3. 用递归、DP、贪心、回溯或图算法解决问题，并说明为什么；
4. 编写边界安全、资源可控、错误可见的 C 程序；
5. 用测试和复杂度分析支撑“程序确实正确、在目标规模下可用”；
6. 将一个算法程序逐步演化为有需求、有设计、有测试、有文档的工程项目。

---

# 第三部分：复盘后的代码能力考察题

## 使用方式

建议先独立完成，不看原实验源码。每题提交：源码、README、复杂度说明和测试记录。评分重点不只是结果，还包括接口设计、错误处理、内存安全和可解释性。

## A 级：基础恢复题

### 题 1：安全的大整数阶乘库

实现 `BigInt` 结构和以下接口：

```c
int bigint_factorial(unsigned n, BigInt *out);
int bigint_to_string(const BigInt *x, char *buf, size_t cap);
void bigint_free(BigInt *x);
```

要求支持至少 `100!`，正确处理容量不足；禁止使用第三方大数库。测试 `0!、1!、40!、100!` 和错误参数。

考察：数组表示、动态内存、接口契约、溢出/容量处理。

### 题 2：健壮地址校验器

实现 `validate_address(input, &type, &error)`，支持题设中的 IPv4、IPv6（不允许 `::`）和 MAC。禁止使用会吞掉空段的简单分词作为唯一校验手段。

考察：词法扫描、错误定位、长度限制、边界输入。

### 题 3：可测试的链表 ADT

实现一个带哨兵头结点的有序双向链表，支持插入、删除全部匹配值、查询位置、正反向遍历和销毁。为每个操作写可自动运行的断言测试。

考察：指针不变量、所有权、空表/单节点边界、测试设计。

## B 级：算法综合题

### 题 4：表达式解释器

在实验 7 基础上支持：

- 整数、括号、`+ - * /`
- 一元负号
- 空白和错误位置报告
- 除零、括号不匹配、操作数不足

要求转换和求值两个阶段互相独立，并为词法错误、语法错误、运行时错误定义不同错误码。

考察：分层设计、栈、词法/语法、错误传播。

### 题 5：动态最短路服务

实现一个有向图模块，支持添加边、删除边、查询指定源点到终点的路径。边权非负；当图未变化时重复查询应复用缓存，图变化后缓存失效。

考察：邻接表、Dijkstra、堆、路径恢复、缓存一致性。

### 题 6：任务排程比较器

输入飞机销售任务或一般带权任务，分别实现：

1. 优先队列贪心方案；
2. 暴力枚举小规模最优方案；
3. 随机生成小数据，比对两者结果。

要求输出反例（若有）并解释贪心成立的条件。

考察：贪心证明、堆、基准/对拍测试、模型边界。

### 题 7：LIS 与背包统一框架

实现一个命令行 DP 工具，用户选择 LIS 或 0-1 背包；两种算法都必须输出最优值和方案。要求抽取公共的输入校验、结果输出和测试框架。

考察：状态设计、回溯、模块复用、避免重复代码。

## C 级：工程能力题

### 题 8：可持久化 Todo v2

在实验 20 基础上增加：

- 稳定且不可复用的任务 ID；
- `add/list/done/undo/delete/search`；
- 内容允许 `|` 和引号；
- 原子保存：先写临时文件，成功后替换正式文件；
- 损坏数据文件时给出行号和错误原因；
- 至少 20 个自动化测试。

提交物：`todo.c`、README、数据格式说明、测试报告、已知限制。

考察：需求演化、文件格式、原子性、错误恢复、回归测试。

### 题 9：学生管理系统的质量升级

将实验 19 改造成可复用模块：

- 头文件与实现文件分离；
- 动态数组支持任意数量记录；
- 学号唯一性检查；
- 稳定排序；
- 文本格式和二进制格式任选其一，但必须写版本号/格式说明；
- 对加载、保存、删除、排序分别写测试。

考察：模块边界、持久化兼容性、测试替身和 API 设计。

### 题 10：文本统计性能实验

实现三种版本：线性数组、哈希表、哈希表 + `fread`。使用同一批逐渐增大的文本，记录运行时间、峰值内存、不同单词数和前 10 结果，写出结论。

必须回答：

- 哈希冲突如何处理？
- 单词跨块如何处理？
- 为什么排序仍然需要额外数组？
- 优化是否改变了统计语义？

考察：性能工程、实验设计、可重复测量、结果解释。

## 评分量表（建议）

| 维度 | 权重 | 达标表现 |
|---|---:|---|
| 功能正确 | 30% | 正常与边界用例结果正确 |
| 数据结构/算法 | 20% | 选择合理，能解释不变量和复杂度 |
| C 安全性 | 20% | 无明显越界、泄漏、悬空指针、未定义行为 |
| 工程结构 | 15% | 模块清晰，接口稳定，错误可见 |
| 测试与文档 | 15% | 有可复现命令、测试数据、限制和结论 |

### 复盘完成判定

当你能在不看原代码的情况下完成 A 级全部题、B 级至少两题、C 级至少一题，并能解释每个设计取舍，才可以认为这套实验已经从“做过”转化为“掌握了软件工程实践能力”。

